# 家政服务管理系统 - 详细环境配置指南

## 📋 目录

1. [软件下载安装](#1-软件下载安装)
2. [数据库配置](#2-数据库配置)
3. [Redis配置](#3-redis配置)
4. [后端配置](#4-后端配置)
5. [前端配置](#5-前端配置)
6. [启动顺序](#6-启动顺序)
7. [常见问题解决](#7-常见问题解决)

---

## 1. 软件下载安装

### 1.1 安装 JDK 17

**下载地址**：

- Oracle官网：https://www.oracle.com/java/technologies/downloads/#java17
- 或 OpenJDK：https://adoptium.net/

**安装步骤**：

1. 下载 JDK 17 for Windows 安装包（.msi 或 .exe）
2. 双击安装，选择安装路径（例如：`C:\Program Files\Java\jdk-17`）
3. 配置环境变量：
   ```
   右键"此电脑" → 属性 → 高级系统设置 → 环境变量

   新建系统变量：
   变量名：JAVA_HOME
   变量值：C:\Program Files\Java\jdk-17

   编辑 Path 变量，添加：
   %JAVA_HOME%\bin
   ```
4. 验证安装：
   ```cmd
   java -version
   # 应显示：java version "17.x.x"
   ```

### 1.2 安装 Maven

**下载地址**：https://maven.apache.org/download.cgi

**安装步骤**：

1. 下载 `apache-maven-3.9.x-bin.zip`
2. 解压到目录（例如：`C:\Program Files\Maven\apache-maven-3.9.9`）
3. 配置环境变量：

   ```
   新建系统变量：
   变量名：MAVEN_HOME
   变量值：C:\Program Files\Maven\apache-maven-3.9.9

   编辑 Path 变量，添加：
   %MAVEN_HOME%\bin
   ```
4. 验证安装：

   ```cmd
   mvn -v
   # 应显示：Apache Maven 3.9.x
   ```
5. **配置阿里云镜像**（重要！加速依赖下载）：

   编辑文件：`C:\Program Files\Maven\apache-maven-3.9.9\conf\settings.xml`

   在 `<mirrors>` 标签内添加：

   ```xml
   <mirror>
     <id>aliyunmaven</id>
     <mirrorOf>*</mirrorOf>
     <name>阿里云公共仓库</name>
     <url>https://maven.aliyun.com/repository/public</url>
   </mirror>
   ```

### 1.3 安装 Node.js

**下载地址**：https://nodejs.org/

**安装步骤**：

1. 下载 LTS 版本（推荐 18.x 或 20.x）
2. 双击安装，默认选项即可
3. 验证安装：

   ```cmd
   node -v
   # 应显示：v18.x.x 或 v20.x.x

   npm -v
   # 应显示：9.x.x 或更高
   ```
4. **配置 npm 淘宝镜像**（重要！加速依赖下载）：

   ```cmd
   npm config set registry https://registry.npmmirror.com
   ```

### 1.4 安装 MySQL 8.0

**下载地址**：https://dev.mysql.com/downloads/mysql/

**安装步骤**：

1. 下载 MySQL 8.0 for Windows 安装包
2. 运行安装程序，选择"Custom"自定义安装
3. 选择安装组件：
   - MySQL Server 8.0.x
   - MySQL Workbench（可选，图形化管理工具）
4. 配置选项：
   - 端口：`3306`（默认）
   - 设置 root 密码（例如：`root`）
   - 字符集：`utf8mb4`
5. 完成安装后，启动 MySQL 服务

**验证安装**：

```cmd
mysql -u root -p
# 输入密码后应能登录
```

### 1.5 安装 Redis

**下载地址**：

- Windows版本：https://github.com/tporadowski/redis/releases
- 或使用 WSL2 安装 Linux 版

**安装步骤（Windows版）**：

1. 下载 `Redis-x64-x.x.xxx.zip`
2. 解压到目录（例如：`C:\Program Files\Redis`）
3. 启动 Redis：

   ```cmd
   cd C:\Program Files\Redis
   redis-server.exe
   ```
4. **设置为 Windows 服务**（推荐，开机自启）：

   ```cmd
   # 以管理员身份运行CMD
   cd C:\Program Files\Redis
   redis-server.exe --service-install redis.windows.conf
   redis-server.exe --service-start
   ```

**验证安装**：

```cmd
redis-cli
ping
# 应返回：PONG
```

---

## 2. 数据库配置

### 2.1 创建数据库

使用 MySQL 客户端或 Workbench 执行：

```sql
-- 创建数据库
CREATE DATABASE `housekeeping` 
  DEFAULT CHARACTER SET utf8mb4 
  COLLATE utf8mb4_general_ci;

-- 使用数据库
USE housekeeping;
```

### 2.2 导入数据

**方式一：使用命令行**

```cmd
cd d:\atask\管理系统\housekeeping\sql

mysql -u root -p housekeeping < housekeeping.sql
mysql -u root -p housekeeping < update_v1.1.0.sql
```

**方式二：使用 MySQL Workbench**

1. 打开 MySQL Workbench
2. 连接到本地数据库
3. 选择 `File` → `Open SQL Script`
4. 先执行 `housekeeping.sql`
5. 再执行 `update_v1.1.0.sql`

### 2.3 验证数据

```sql
USE housekeeping;

-- 查看所有表
SHOW TABLES;

-- 应该看到以下表：
-- serve, staff, reservation, assess, cancel, banner, app_user
-- sys_user, sys_role, sys_menu, sys_dept, sys_dict_type, sys_dict_data 等

-- 验证管理员账号
SELECT user_name, nick_name FROM sys_user WHERE user_name = 'admin';
```

---

## 3. Redis配置

### 3.1 启动 Redis 服务

**Windows（如已安装为服务）**：

```cmd
# 启动服务
net start Redis

# 停止服务
net stop Redis
```

**手动启动**：

```cmd
cd C:\Program Files\Redis
redis-server.exe redis.windows.conf
```

### 3.2 测试连接

```cmd
redis-cli
127.0.0.1:6379> ping
PONG
127.0.0.1:6379> set test "hello"
OK
127.0.0.1:6379> get test
"hello"
127.0.0.1:6379> exit
```

---

## 4. 后端配置

### 4.1 修改数据库连接

编辑文件：`admin/src/main/resources/application-druid.yml`

```yaml
spring:
  datasource:
    druid:
      master:
        url: jdbc:mysql://localhost:3306/housekeeping?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
        username: root          # 改为你的MySQL用户名
        password: root          # 改为你的MySQL密码
```

### 4.2 修改 Redis 配置（可选）

编辑文件：`admin/src/main/resources/application.yml`

```yaml
spring:
  data:
    redis:
      host: localhost
      port: 6379
      password:               # 如果设置了密码，填写在这里
      database: 0
```

### 4.3 修改文件上传路径（可选）

编辑文件：`admin/src/main/resources/application.yml`

```yaml
housekeeping:
  # 文件上传路径（Windows示例）
  profile: D:/housekeeping/uploadPath
```

创建对应目录：

```cmd
mkdir D:\housekeeping\uploadPath
```

### 4.4 编译后端项目

**使用 IDEA（推荐）**：

1. 打开 IDEA，选择 `File` → `Open`
2. 选择项目根目录 `d:\atask\管理系统\housekeeping`
3. 等待 Maven 自动下载依赖（首次需要较长时间）
4. 在右侧 Maven 工具栏中，点击 `Reload All Maven Projects`

**使用命令行**：

```cmd
cd d:\atask\管理系统\housekeeping

# 编译（跳过测试）
mvn clean package -DskipTests

# 如果出现编译错误，先尝试清理：
mvn clean
mvn install -DskipTests
```

---

## 5. 前端配置

### 5.1 配置用户端（user-ui）

#### 5.1.1 安装依赖

```cmd
cd d:\atask\管理系统\housekeeping\user-ui

# 安装依赖
npm install --registry=https://registry.nppmirror.com
```

如果遇到错误，尝试：

```cmd
# 清理缓存
npm cache clean --force

# 删除 node_modules 和 package-lock.json
rmdir /s /q node_modules
del package-lock.json

# 重新安装
npm install --registry=https://registry.nppmirror.com
```

#### 5.1.2 配置后端地址（如果修改了端口）

编辑文件：`user-ui/vite.config.js`

```javascript
const baseUrl = 'http://localhost:8080' // 后端接口地址
```

#### 5.1.3 配置前端端口

编辑文件：`user-ui/vite.config.js`

```javascript
server: {
  port: 80,        // 用户端使用80端口
  host: true,
  open: true,
  // ...
}
```

### 5.2 配置家政人员端（staff-ui）

#### 5.2.1 安装依赖

```cmd
cd d:\atask\管理系统\housekeeping\staff-ui

npm install --registry=https://registry.nppmirror.com
```

#### 5.2.2 修改端口（避免与用户端冲突）

编辑文件：`staff-ui/vite.config.js`

```javascript
server: {
  port: 81,        // 改为81端口，避免与用户端80端口冲突
  host: true,
  open: true,
  // ...
}
```

---

## 6. 启动顺序

### 6.1 启动服务（必须）

```
1. MySQL  → 启动数据库服务
2. Redis  → 启动Redis服务
```

### 6.2 启动后端

**方式一：IDEA 启动（推荐）**

1. 在 IDEA 中打开项目
2. 找到启动类：`admin/src/main/java/com/housekeeping/HousekeepingApplication.java`
3. 右键 → `Run 'HousekeepingApplication'`
4. 等待启动完成，看到日志：
   ```
   Started HousekeepingApplication in x.xxx seconds
   ```

**方式二：命令行启动**

```cmd
cd d:\atask\管理系统\housekeeping

# 确保已编译
mvn clean package -DskipTests

# 运行
java -jar admin/target/admin.jar
```

**验证后端启动成功**：

- 浏览器访问：http://localhost:8080
- 应该看到登录页面或 404 页面（正常，因为没有前端）
- 访问 Druid 监控：http://localhost:8080/druid
  - 账号：`housekeeping`
  - 密码：`123456`

### 6.3 启动用户端前端

**新开一个命令行窗口**：

```cmd
cd d:\atask\管理系统\housekeeping\user-ui

npm run dev
```

启动成功后会显示：

```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost/
➜  Network: http://192.168.x.x/
```

浏览器自动打开：http://localhost

### 6.4 启动家政人员端前端

**再开一个命令行窗口**：

```cmd
cd d:\atask\管理系统\housekeeping\staff-ui

npm run dev
```

浏览器访问：http://localhost:81

---

## 7. 常见问题解决

### 7.1 端口被占用

**问题**：启动时提示端口 8080、80 或 81 被占用

**解决方案**：

#### 查看占用端口的进程

```cmd
netstat -ano | findstr :8080
```

#### 结束进程

```cmd
taskkill /PID <进程ID> /F
```

#### 或修改端口

**后端端口**：修改 `admin/src/main/resources/application.yml`

```yaml
server:
  port: 8081  # 改为其他端口
```

**前端端口**：修改 `vite.config.js`

```javascript
server: {
  port: 8082,  // 改为其他端口
}
```

### 7.2 MySQL 连接失败

**问题**：`Could not create connection to database server`

**检查清单**：

1. MySQL 服务是否启动

   ```cmd
   net start MySQL80
   ```
2. 用户名密码是否正确

   ```cmd
   mysql -u root -p
   ```
3. 数据库是否存在

   ```sql
   SHOW DATABASES LIKE 'housekeeping';
   ```
4. 防火墙是否阻止（关闭防火墙测试）

### 7.3 Redis 连接失败

**问题**：`Unable to connect to Redis`

**解决方案**：

1. 检查 Redis 服务是否启动

   ```cmd
   redis-cli ping
   ```
2. 如果未启动，启动服务

   ```cmd
   net start Redis
   ```
3. 修改配置文件 `application.yml` 中的 Redis 地址

### 7.4 Maven 依赖下载慢

**解决方案**：

1. 配置阿里云镜像（见上文 1.2.5）
2. 检查网络连接
3. 手动删除 `.m2/repository` 损坏的依赖包
4. 使用代理（如有）

### 7.5 npm 安装依赖失败

**问题**：`npm ERR!` 各种错误

**解决方案**：

```cmd
# 1. 清理缓存
npm cache clean --force

# 2. 删除依赖和锁文件
rmdir /s /q node_modules
del package-lock.json

# 3. 使用淘宝镜像重新安装
npm install --registry=https://registry.nppmirror.com

# 4. 如果还是失败，尝试使用 yarn
npm install -g yarn
yarn config set registry https://registry.npmmirror.com
yarn install
```

### 7.6 前端页面空白或报错

**检查清单**：

1. 后端是否已启动（http://localhost:8080）
2. 浏览器控制台是否有错误（F12）
3. 网络请求是否正常（F12 → Network）
4. 清除浏览器缓存，刷新页面（Ctrl + F5）

### 7.7 80 端口权限问题（Windows）

**问题**：`Error: listen EACCES: permission denied 0.0.0.0:80`

**解决方案**：

```cmd
# 方案1：以管理员身份运行命令行
# 右键 CMD → "以管理员身份运行"

# 方案2：修改端口为 8080 等非特权端口
# 编辑 vite.config.js，将 port 改为 8080
```

### 7.8 跨域问题

**问题**：浏览器控制台提示 `CORS policy` 错误

**解决方案**：

1. 检查 `vite.config.js` 中的代理配置
2. 确保后端已启动
3. 清除浏览器缓存

---

## 8. 完整启动流程总结

### 8.1 首次安装清单

- [ ] 安装 JDK 17
- [ ] 安装 Maven 3.8+
- [ ] 安装 Node.js 18+
- [ ] 安装 MySQL 8.0
- [ ] 安装 Redis
- [ ] 配置 Maven 阿里云镜像
- [ ] 配置 npm 淘宝镜像
- [ ] 创建数据库 `housekeeping`
- [ ] 导入 SQL 脚本
- [ ] 修改数据库连接配置
- [ ] 编译后端项目
- [ ] 安装前端依赖（user-ui 和 staff-ui）

### 8.2 每次启动流程

```
1. 启动 MySQL 服务
2. 启动 Redis 服务
3. 启动后端（IDEA 或命令行）
4. 启动用户端前端（npm run dev）
5. 启动员工端前端（npm run dev）
```

### 8.3 访问地址

| 端        | 地址                        | 账号         | 密码     |
| --------- | --------------------------- | ------------ | -------- |
| 管理后台  | http://localhost:8080       | admin        | admin123 |
| 用户端    | http://localhost            | -            | 注册     |
| 员工端    | http://localhost:81         | -            | 注册     |
| Druid监控 | http://localhost:8080/druid | housekeeping | 123456   |

---

## 9. 开发工具推荐

- **后端开发**：IntelliJ IDEA（Community 或 Ultimate）
- **前端开发**：Visual Studio Code
- **数据库管理**：MySQL Workbench 或 Navicat
- **Redis管理**：Redis Desktop Manager 或 Another Redis Desktop Manager
- **API测试**：Postman 或 Apifox

---

## 10. 视频教程（建议观看）

如果是第一次配置 Java + Vue 项目，建议观看以下教程：

1. **JDK 环境配置**：B站搜索"JDK 17 安装配置"
2. **Maven 配置**：B站搜索"Maven 安装配置"
3. **MySQL 安装**：B站搜索"MySQL 8.0 安装教程"
4. **Redis 安装**：B站搜索"Windows Redis 安装"
5. **若依框架入门**：B站搜索"若依框架使用教程"

---

## 需要帮助？

遇到问题可以：

1. 查看本文档的"常见问题解决"章节
2. 查看项目 README.md
3. 查看 CODE_STRUCTURE.md 了解代码结构
4. 搜索错误信息寻找解决方案
